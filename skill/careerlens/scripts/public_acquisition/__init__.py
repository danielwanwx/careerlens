"""Portable, public-only job-evidence acquisition bundle.

The vendored modules in this package are copied byte-for-byte from the
documented CareerOS public-acquisition subset.  ``server`` is the thin local
loopback boundary used by CareerLens; it does not persist runs to disk or accept
credentials from a browser.
"""
