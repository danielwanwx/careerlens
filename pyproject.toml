[project]
name = "careerlens"
version = "0.1.0"
description = "Evidence-bounded career diagnosis and preparation runbook skill"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "Apache-2.0"}

[tool.pytest.ini_options]
testpaths = ["tests"]
