# Security and privacy

Report vulnerabilities through GitHub's private vulnerability reporting when
available. Do not open a public issue containing candidate material, secrets,
or exploit details.

The skill treats candidate artifacts as sensitive input. Generated runbooks
should retain compact evidence references rather than raw source text. Validate
artifacts before sharing them, and review them manually before publication.

Supported security updates currently apply to the latest `0.x` release.
